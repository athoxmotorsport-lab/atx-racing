(() => {
  const panel=document.querySelector('[data-ranking-panel="driver"]'),select=document.querySelector('[data-driver-ranking-select]'),grid=document.querySelector('[data-driver-circuit-grid]');
  if(!panel||!select||!grid)return;
  const base='https://twjpjzalyvbsdpbzhqln.supabase.co/functions/v1'; let payload=null,sectors=[];
  const fmt=v=>{const ms=Number(v);if(!Number.isFinite(ms)||ms<=0)return'—';return `${Math.floor(ms/60000)}:${String(Math.floor(ms%60000/1000)).padStart(2,'0')}.${String(Math.floor(ms%1000)).padStart(3,'0')}`};
  const fmtSector=v=>{const ms=Number(v);if(!Number.isFinite(ms)||ms<=0)return'—';return `${Math.floor(ms/1000)}.${String(Math.floor(ms%1000)).padStart(3,'0')}`};
  const resultFor=(c,id)=>(c?.drivers||[]).find(x=>x.driver_id===id);
  const sectorFor=(id,key)=>sectors.find(x=>x.driver_id===id&&x.circuit_key===key)||{};
  function shell(){let s=panel.querySelector('.driver-premium-shell');if(s)return s;s=document.createElement('div');s.className='driver-premium-shell';s.innerHTML=`<section class="driver-premium-hero"><div class="driver-premium-kicker">ATX // DRIVER PERFORMANCE</div><div class="driver-premium-title"><span>PAR</span> <strong>PILOTE</strong></div><p>Sélectionnez un pilote pour comparer ses chronos circuit par circuit et ses meilleurs secteurs.</p></section><section class="driver-premium-control"><div class="driver-premium-select-slot"></div><div class="driver-premium-identity" data-driver-premium-identity></div></section><section class="driver-premium-stats" data-driver-premium-stats></section><section class="driver-track-list" data-driver-track-list></section>`;panel.querySelector('.ranking-panel-head')?.setAttribute('hidden','');grid.hidden=true;panel.prepend(s);s.querySelector('.driver-premium-select-slot').append(select.closest('label')||select);return s;}
  function render(){if(!payload||!select.value)return;const s=shell(),driver=(payload.drivers||[]).find(d=>d.driver_id===select.value);if(!driver)return;const timed=(payload.circuits||[]).filter(c=>Number(resultFor(c,driver.driver_id)?.best_lap_ms)>0);
    const identity=s.querySelector('[data-driver-premium-identity]');identity.replaceChildren();const avatar=document.createElement('span');avatar.className='driver-premium-avatar';if(driver.avatar_url){const i=document.createElement('img');i.src=driver.avatar_url;i.alt='';i.referrerPolicy='no-referrer';avatar.append(i)}else avatar.textContent=String(driver.display_name||'AT').slice(0,2).toUpperCase();const info=document.createElement('div');info.innerHTML='<strong></strong><small></small>';info.querySelector('strong').textContent=driver.display_name||'ACC Driver';info.querySelector('small').textContent=driver.team_name||'ATX Racing Driver';const badge=document.createElement('span');badge.className='driver-premium-tier';badge.dataset.tier=driver.performance_class||'unranked';badge.textContent=String(driver.performance_class||'UNRANKED').toUpperCase();identity.append(avatar,info,badge);
    s.querySelector('[data-driver-premium-stats]').innerHTML=`<article><small>Circuits chronométrés</small><strong>${timed.length} / ${(payload.circuits||[]).length}</strong></article><article><small>Performance moyenne</small><strong>${Number.isFinite(Number(driver.performance_score))?Number(driver.performance_score).toFixed(2)+'%':'—'}</strong></article><article><small>Courses</small><strong>${driver.races??0}</strong></article><article><small>SAFE</small><strong>${driver.safety_score!=null?Number(driver.safety_score).toFixed(0)+'%':'—'}</strong></article>`;
    const out=s.querySelector('[data-driver-track-list]');out.replaceChildren();(payload.circuits||[]).forEach((c,index)=>{const r=resultFor(c,driver.driver_id),sec=sectorFor(driver.driver_id,c.circuit_key),source=document.querySelectorAll('[data-ranking-circuits] .circuit-visual-card')[index],img=source?.querySelector('.circuit-card-media img')?.src||'atx-racing-background.webp',country=source?.querySelector('.circuit-card-country')?.textContent||'ACC';const row=document.createElement('article');row.className=`driver-track-row ${r?.best_lap_ms?'has-time':'no-time'}`;if(source)['--flag-a','--flag-b','--flag-c'].forEach(v=>row.style.setProperty(v,source.style.getPropertyValue(v)));row.innerHTML=`<div class="driver-track-media"><img alt="" loading="lazy"><span></span></div><div class="driver-track-name"><small>Circuit</small><strong></strong><em></em></div><div class="driver-track-time"><small>Meilleur tour</small><strong>${r?.best_lap_ms?fmt(r.best_lap_ms):'—'}</strong><em>${r?.best_lap_ms?`${String(r.session_type||'—').toUpperCase()} · ${r.pace_percent?Number(r.pace_percent).toFixed(2)+'%':'—'}`:'Circuit à compléter'}</em></div><div class="driver-track-sectors"><div><small>S1</small><b>${fmtSector(sec.best_sector_1_ms)}</b></div><div><small>S2</small><b>${fmtSector(sec.best_sector_2_ms)}</b></div><div><small>S3</small><b>${fmtSector(sec.best_sector_3_ms)}</b></div></div>`;row.querySelector('img').src=img;row.querySelector('.driver-track-media span').textContent=country;row.querySelector('.driver-track-name strong').textContent=c.circuit_name;row.querySelector('.driver-track-name em').textContent=r?.performance_class?String(r.performance_class).toUpperCase():'—';out.append(row)});
  }
  Promise.all([fetch(`${base}/public-leaderboard`,{cache:'no-store'}).then(r=>r.json()),fetch(`${base}/public-driver-sectors`,{cache:'no-store'}).then(r=>r.ok?r.json():{sectors:[]})]).then(([p,s])=>{payload=p;sectors=s.sectors||[];shell();const wait=()=>select.options.length?render():setTimeout(wait,80);wait();select.addEventListener('change',render);new MutationObserver(()=>{if(!panel.hidden)render()}).observe(panel,{attributes:true,attributeFilter:['hidden']})}).catch(()=>{});
})();